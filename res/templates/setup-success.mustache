<!DOCTYPE html>
<html>
<head>
    <title>Commit Bot</title>

    <meta name="twitter:card" content="summary"/>
    <meta name="twitter:site" content="@kelunik"/>
    <meta name="twitter:creator" content="@kelunik"/>
    <meta property="og:url" content="/"/>
    <meta property="og:title" content="Commit Bot"/>
    <meta property="og:description"
          content="Commit Bot helps you to keep your Git commit history clean by enforcing several rules."/>
    <meta name="viewport" content="width=device-width,initial-scale=1.0,user-scalable=no"/>

    <link rel="stylesheet" href="//fonts.googleapis.com/css?family=Bitter|Source+Code+Pro:300,400,700">
    <link rel="stylesheet" href="/css/screen.css">
</head>
<body>
<div class="top">
    {{^user.id}}
        <a href="/login">
            Login
        </a>
    {{/user.id}}
    {{#user.id}}
        <form action="/logout" method="POST">
            <button type="submit" class="logout">
                Logout
            </button>
        </form>
    {{/user.id}}
</div>

<main class="content">
    <h1>Setup Repository</h1>

    <p class="help-text">
        Setup complete.
    </p>

    <p class="help-text">
        <a href="/">Back to dashboard</a>.
    </p>
</main>
</body>
</html>